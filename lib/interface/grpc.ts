import {iinterface} from "../iinterface";

export const grpc:iinterface = {
    identifier: "GRPC",
    name: "grpc",
    init: initialize,
    bind: bind,
    middleware: middleware
}

function initialize(){

}

function bind(){

}

function middleware(){

}